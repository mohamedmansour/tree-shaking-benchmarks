{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "outDir": "../dist/infra",     // Output directory for infra code.
        "resolveJsonModule": true      // Enable JSON module resolution.
    }
}
